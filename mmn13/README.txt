Build compiler for imaginary assembler for a computer working on 12 bits.
Input should be <fileName>.as
If input is correct Output should be:

<fileName>.ent - containt original code but no blank rows, macros spreaded, no comments
<fileName>.ext - table of var and its adress
<fileName>.ob - binary code translated to base64


else output should be error messages as regular compiler